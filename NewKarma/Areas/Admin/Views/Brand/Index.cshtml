@model ReflectionIT.Mvc.Paging.PagingList<Brand>
@using ReflectionIT.Mvc.Paging
@addTagHelper*,ReflectionIT.Mvc.Paging 
@{
    ViewData["Title"] = "مدیریت برند";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header bg-light">@ViewData["Title"]</div>
            <div class="card-body">
                <p>
                    <a asp-area="Admin" asp-controller="Brand" asp-action="Create" class="btn btn-primary">افزودن برند جدید</a>
                </p>
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>
                                @Html.SortableHeaderFor(model => model.Title)
                            </th>
                            <th class="text-center">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Title)
                                </td>
                                <td class="text-center">
                                    <a asp-area="Admin" asp-controller="Brand" asp-action="Edit" asp-route-id="@item.BrandId" class="btn btn-success btn-icon">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <a asp-area="Admin" asp-controller="Brand" asp-action="Delete" asp-route-id="@item.BrandId" class="btn btn-danger btn-icon">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <nav>
                    @await this.Component.InvokeAsync("Pager", new { PagingList = this.Model })
                </nav>
            </div>
        </div>
    </div>
</div>